# v0.5 — Day 5：LangChain Agent 重构

> **版本目标**：用 LangChain 重写 Agent，理解框架如何简化工具编排，并与手写 ReAct 对比。

---

## 今日收获

- LangChain 核心概念：Model、Tool、Agent、AgentExecutor
- 框架 vs 原生 API 的取舍
- 为 Day 6 RAG 做铺垫（LlamaIndex / LangChain 向量能力）

---

## 为什么引入框架？

| 手写 ReAct | LangChain |
|------------|-----------|
| 完全可控、易理解 | 开箱即用、生态丰富 |
| 代码随功能增长变复杂 | 工具/RAG/记忆有标准抽象 |
| 适合学习原理 | 适合快速迭代产品 |

**策略**：先手写（v0.3–v0.4）理解原理，再用框架（v0.5+）提效。

---

## 步骤 1：安装 LangChain

更新 `requirements.txt`：

```txt
openai>=1.40.0
python-dotenv>=1.0.0
rich>=13.7.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
```

```powershell
pip install -r requirements.txt
```

---

## 步骤 2：用 LangChain 定义工具

创建 `agent/langchain_agent.py`：

```python
# agent/langchain_agent.py
import os
from datetime import datetime
from pathlib import Path

from dotenv import load_dotenv
from langchain_openai import ChatOpenAI
from langchain_core.tools import tool
from langchain.agents import create_tool_calling_agent, AgentExecutor
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder

load_dotenv()

PROJECT_ROOT = Path(__file__).resolve().parent.parent


@tool
def get_current_time() -> str:
    """获取当前日期和时间。用户问几点、今天几号时使用。"""
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")


@tool
def read_file(path: str) -> str:
    """读取项目内文本文件。path 为相对项目根的路径，如 knowledge/project-intro.md。"""
    target = (PROJECT_ROOT / path).resolve()
    if not str(target).startswith(str(PROJECT_ROOT)):
        return "错误：不允许访问项目外路径"
    if not target.exists():
        return f"错误：文件不存在 {path}"
    return target.read_text(encoding="utf-8")[:8000]


@tool
def list_files(path: str = ".") -> str:
    """列出目录下的文件名。path 为相对路径，如 knowledge。"""
    target = (PROJECT_ROOT / path).resolve()
    if not str(target).startswith(str(PROJECT_ROOT)):
        return "错误：不允许访问项目外路径"
    if not target.is_dir():
        return f"错误：目录不存在 {path}"
    return ", ".join(f.name for f in target.iterdir()) or "（空目录）"


TOOLS = [get_current_time, read_file, list_files]


def build_agent_executor(verbose: bool = True) -> AgentExecutor:
    llm = ChatOpenAI(
        model=os.getenv("OPENAI_MODEL", "deepseek-chat"),
        api_key=os.getenv("OPENAI_API_KEY"),
        base_url=os.getenv("OPENAI_BASE_URL"),
        temperature=0,
    )

    prompt = ChatPromptTemplate.from_messages([
        ("system", "你是一个能使用工具的 AI 助手，请用中文回答。"),
        MessagesPlaceholder("chat_history", optional=True),
        ("human", "{input}"),
        MessagesPlaceholder("agent_scratchpad"),
    ])

    agent = create_tool_calling_agent(llm, TOOLS, prompt)
    return AgentExecutor(agent=agent, tools=TOOLS, verbose=verbose)


def run_agent(user_input: str, chat_history: list | None = None) -> str:
    executor = build_agent_executor(verbose=True)
    result = executor.invoke({
        "input": user_input,
        "chat_history": chat_history or [],
    })
    return result["output"]
```

---

## 步骤 3：在 main.py 中切换实现

可通过环境变量或命令行参数切换：

```python
# main.py 片段
import os
USE_LANGCHAIN = os.getenv("USE_LANGCHAIN", "1") == "1"

if USE_LANGCHAIN:
    from agent.langchain_agent import run_agent
    # 循环内: reply = run_agent(user_input)
else:
    from agent.react_agent import ReactAgent
    agent = ReactAgent()
    # 循环内: reply = agent.run(user_input)
```

---

## 步骤 4：运行对比测试

```powershell
# LangChain 版
$env:USE_LANGCHAIN="1"; python main.py

# 手写 ReAct 版
$env:USE_LANGCHAIN="0"; python main.py
```

用相同问题对比两者行为和日志。

---

## 核心概念

| LangChain 概念 | 对应 v0.4 |
|----------------|-----------|
| `@tool` 装饰器 | `TOOL_SCHEMA` + 函数 |
| `create_tool_calling_agent` | `chat_with_tools` 循环 |
| `AgentExecutor` | `ReactAgent.run` |
| `agent_scratchpad` | tool 消息的中间状态 |
| `MessagesPlaceholder` | 对话历史插槽 |

---

## 验收清单

- [ ] LangChain 版三个工具均正常
- [ ] `verbose=True` 能看到 Agent 思考链
- [ ] 能通过环境变量切换两种实现
- [ ] 能说出框架帮你省了哪些代码
- [ ] 理解 `@tool` 的 docstring 会作为工具描述

---

## 常见问题

| 问题 | 解决 |
|------|------|
| `ImportError: langchain` | 重新 `pip install -r requirements.txt` |
| Agent 不调用工具 | `temperature=0`；检查 `@tool` docstring |
| 版本 API 变化 | LangChain 迭代快，以官方文档为准 |

---

## 学习笔记（自行填写）

```
日期：
耗时：
框架版 vs 手写版代码行数对比：
```

---

## 下一版本预告

**v0.6**：加入 **RAG**，让 Agent 从向量库检索知识，而不是每次读全文件。
